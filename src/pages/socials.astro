---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Brandon Walker | Social Media & Projects</title>
		<meta name="description" content="Connect with Brandon Walker on social media platforms and explore his projects." />
		<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
		<script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js" async id="particles-script" onerror="console.error('Failed to load particles.js')"></script>
		<style>
  body {
    background: #0f172a;
    color: #e2e8f0;
    font-family: 'Inter', sans-serif;
    line-height: 1.7;
    overflow-x: hidden;
    position: relative;
  }
  #particles-js {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
    opacity: 1;
  }
  .content-section {
    opacity: 0;
    transition: opacity 0.8s ease-out;
  }
  .content-section.visible {
    opacity: 1;
  }
  .section-title {
    color: #7c3aed;
    font-family: 'Playfair Display', serif;
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;
    text-shadow: 0 0 10px rgba(124, 58, 237, 0.5);
  }
  .section-title::after {
    content: '';
    position: absolute;
    width: 60%;
    height: 4px;
    background: linear-gradient(to right, #7c3aed, #4f46e5);
    bottom: -0.75rem;
    left: 20%;
    border-radius: 2px;
  }
  .card {
    background: rgba(30, 27, 75, 0.9);
    border-radius: 1.5rem;
    padding: 2.5rem;
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
    transition: transform 0.4s ease, box-shadow 0.4s ease;
    backdrop-filter: blur(5px);
  }
  .card:hover {
    transform: translateY(-15px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6);
  }
  .social-card {
    background: rgba(30, 27, 75, 0.9);
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    backdrop-filter: blur(5px);
    text-align: center;
  }
  .social-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.6);
  }
  .social-icon {
    width: 60px;
    height: 60px;
    margin: 0 auto 1rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    transition: transform 0.3s ease;
  }
  .social-icon:hover {
    transform: scale(1.1);
  }
  .discord-icon {
    background: linear-gradient(45deg, #5865f2, #7289da);
  }
  .github-icon {
    background: linear-gradient(45deg, #333, #6e7681);
  }
  .email-icon {
    background: linear-gradient(45deg, #ea4335, #ff6b6b);
  }
  .phone-icon {
    background: linear-gradient(45deg, #10b981, #34d399);
  }
  .link-icon {
    background: linear-gradient(45deg, #7c3aed, #a855f7);
  }
  .glow-link {
    color: #c4b5fd;
    text-decoration: none;
    position: relative;
    transition: color 0.3s ease;
  }
  .glow-link:hover {
    color: #e879f9;
    text-shadow: 0 0 10px #7c3aed, 0 0 20px #7c3aed;
  }
  .glow-link::after {
    content: '';
    position: absolute;
    width: 0;
    height: 2px;
    background: #7c3aed;
    left: 0;
    bottom: -2px;
    transition: width 0.3s ease;
  }
  .glow-link:hover::after {
    width: 100%;
  }
  .project-card {
    background: rgba(30, 27, 75, 0.9);
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    backdrop-filter: blur(5px);
    margin-bottom: 1.5rem;
  }
  .project-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.6);
  }
  .project-title {
    color: #7c3aed;
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  .project-description {
    color: #e2e8f0;
    margin-bottom: 1rem;
  }
  .project-link {
    display: inline-block;
    background: linear-gradient(45deg, #7c3aed, #4f46e5);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .project-link:hover {
    transform: scale(1.05);
    box-shadow: 0 0 15px rgba(124, 58, 237, 0.7);
  }
</style>
	</head>
	<body>
		<div id="particles-js"></div>
		<Header />
		<main>
			<section class="content-section py-20 px-4 md:px-8">
				<div class="max-w-7xl mx-auto">
					<h2 class="section-title">Social Media</h2>
					<p class="text-lg text-gray-300 mb-8 text-center">Connect with me across different platforms</p>

					<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
						<div class="social-card">
							<div class="social-icon discord-icon">
								<span>💬</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">Discord</h3>
							<p class="text-gray-300 mb-4">Let's chat and collaborate</p>
							<a href="https://discord.com/users/water_wind_" class="glow-link">water_wind_</a>
						</div>

						<div class="social-card">
							<div class="social-icon github-icon">
								<span>💻</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">GitHub</h3>
							<p class="text-gray-300 mb-4">Check out my code repositories</p>
							<a href="https://github.com/Water-Wind-Official" class="glow-link">Water-Wind-Official</a>
						</div>

						<div class="social-card">
							<div class="social-icon email-icon">
								<span>📧</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">Email</h3>
							<p class="text-gray-300 mb-4">Personal correspondence</p>
							<a href="mailto:blwaterwind@gmail.com" class="glow-link">blwaterwind@gmail.com</a>
						</div>

						<div class="social-card">
							<div class="social-icon email-icon">
								<span>💼</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">Work Email</h3>
							<p class="text-gray-300 mb-4">Professional inquiries</p>
							<a href="mailto:sanctumnetwork.1@gmail.com" class="glow-link">sanctumnetwork.1@gmail.com</a>
						</div>

						<div class="social-card">
							<div class="social-icon phone-icon">
								<span>📱</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">Phone</h3>
							<p class="text-gray-300 mb-4">Text only</p>
							<a href="tel:+19127228884" class="glow-link">+1 912-722-8884</a>
						</div>

						<div class="social-card">
							<div class="social-icon link-icon">
								<span>🌐</span>
							</div>
							<h3 class="text-xl font-semibold text-white mb-2">Community</h3>
							<p class="text-gray-300 mb-4">Sanctum Network Discord</p>
							<a href="https://discord.gg/G9baugNyvD" class="glow-link">Join Community</a>
						</div>
					</div>
				</div>
			</section>

			<section class="content-section py-20 px-4 md:px-8">
				<div class="max-w-7xl mx-auto">
					<h2 class="section-title">Projects</h2>
					<p class="text-lg text-gray-300 mb-8 text-center">Some of my notable work and contributions</p>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
						<div class="project-card">
							<h3 class="project-title">Full-Stack Website Development</h3>
							<p class="project-description">Built a complete website from scratch on an empty Linux VM using Django framework, showcasing full-stack development skills with user authentication, inventory management, and transaction processing.</p>
							<a href="#" class="project-link">View Details</a>
						</div>

						<div class="project-card">
							<h3 class="project-title">MySQL Database Design</h3>
							<p class="project-description">Designed and implemented a comprehensive MySQL database for a webshop application, managing user accounts, authentication, inventory tracking, pricing, and transaction records.</p>
							<a href="#" class="project-link">View Details</a>
						</div>

						<div class="project-card">
							<h3 class="project-title">Quality Assurance Automation</h3>
							<p class="project-description">Developed and implemented testing protocols for AI agent systems at TELUS Digital, improving system reliability and performance through rigorous quality assurance processes.</p>
							<a href="#" class="project-link">View Details</a>
						</div>

						<div class="project-card">
							<h3 class="project-title">Portfolio Website</h3>
							<p class="project-description">Created this responsive portfolio website using Astro, featuring modern design, interactive elements, and showcasing professional experience and skills.</p>
							<a href="/" class="project-link">View Site</a>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
		<script>
  function initializeParticles() {
    if (typeof window.particlesJS === 'undefined') {
      console.error('particlesJS is not available');
      return;
    }
    particlesJS('particles-js', {
      particles: {
        number: { value: 80, density: { enable: true, value_area: 800 } },
        color: { value: '#7c3aed' },
        shape: {
          type: 'circle',
          stroke: { width: 0, color: '#000000' },
          polygon: { nb_sides: 5 },
        },
        opacity: {
          value: 0.5,
          random: true,
          anim: { enable: true, speed: 1, opacity_min: 0.1, sync: false },
        },
        size: {
          value: 3,
          random: true,
          anim: { enable: false, speed: 40, size_min: 0.1, sync: false },
        },
        line_linked: {
          enable: true,
          distance: 150,
          color: '#7c3aed',
          opacity: 0.4,
          width: 1,
        },
        move: {
          enable: true,
          speed: 3,
          direction: 'none',
          random: false,
          straight: false,
          out_mode: 'out',
          bounce: false,
          attract: { enable: false, rotateX: 600, rotateY: 1200 },
        },
      },
      interactivity: {
        detect_on: 'canvas',
        events: {
          onhover: { enable: true, mode: 'grab' },
          onclick: { enable: true, mode: 'push' },
          resize: true,
        },
        modes: {
          grab: { distance: 140, line_linked: { opacity: 1 } },
          bubble: { distance: 400, size: 40, duration: 2, opacity: 8, speed: 3 },
          repulse: { distance: 200, duration: 0.4 },
          push: { particles_nb: 4 },
          remove: { particles_nb: 2 },
        },
      },
      retina_detect: true,
    });
  }

  document.addEventListener('DOMContentLoaded', () => {
    const script = document.getElementById('particles-script');
    if (script) {
      script.onload = initializeParticles;
    } else {
      console.error('Particles script not found');
    }
  });

  window.addEventListener('load', () => {
    const sections = document.querySelectorAll('.content-section');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });
    sections.forEach(section => observer.observe(section));
  });
</script>
	</body>
</html>
